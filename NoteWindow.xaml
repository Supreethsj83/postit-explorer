<Window x:Class="PostItExplorer.Views.NoteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Post-it" Height="320" Width="420" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Target:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="PathBox" Width="320" IsReadOnly="True"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,8">
            <TextBlock Text="Color:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox x:Name="ColorBox" Width="120">
                <ComboBoxItem Content="Yellow" IsSelected="True"/>
                <ComboBoxItem Content="Blue"/>
                <ComboBoxItem Content="Green"/>
                <ComboBoxItem Content="Pink"/>
                <ComboBoxItem Content="Orange"/>
            </ComboBox>
            <TextBlock Text=" Label:" VerticalAlignment="Center" Margin="12,0,8,0"/>
            <TextBox x:Name="LabelBox" Width="140" />
        </StackPanel>

        <TextBox x:Name="BodyBox" Grid.Row="2" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>

        <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Right">
            <Button Content="Save" Width="80" Margin="0,0,8,0" Click="Save_Click"/>
            <Button Content="Cancel" Width="80" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
