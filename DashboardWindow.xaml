<Window x:Class="PostItExplorer.Views.DashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Post-it Dashboard" Height="540" Width="820" WindowStartupLocation="CenterScreen">
    <DockPanel>
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="8">
            <TextBox x:Name="SearchBox" Width="260" Margin="0,0,8,0" />
            <ComboBox x:Name="ColorFilter" Width="120" Margin="0,0,8,0">
                <ComboBoxItem Content="Any" IsSelected="True"/>
                <ComboBoxItem Content="Yellow"/>
                <ComboBoxItem Content="Blue"/>
                <ComboBoxItem Content="Green"/>
                <ComboBoxItem Content="Pink"/>
                <ComboBoxItem Content="Orange"/>
            </ComboBox>
            <Button Content="Apply" Width="80" Click="Apply_Click"/>
        </StackPanel>
        <DataGrid x:Name="Grid" AutoGenerateColumns="False" Margin="8" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Color" Binding="{Binding Color}" Width="80"/>
                <DataGridTextColumn Header="Label" Binding="{Binding Label}" Width="120"/>
                <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="*"/>
                <DataGridTextColumn Header="Body" Binding="{Binding Body}" Width="2*"/>
                <DataGridTemplateColumn Header="Actions" Width="180">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Open" Margin="0,0,6,0" Click="Open_Click"/>
                                <Button Content="Edit" Margin="0,0,6,0" Click="Edit_Click"/>
                                <Button Content="Sync Now" Click="Sync_Click"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
